# Enter the virtual Nixos shell using nix develop and the flake.nix
use flake

# Watch files for changes and reload the environment
watch_file pyproject.toml
watch_file flake.nix
watch_file flake.lock

echo "Nix Develop Python environment loaded!!"
